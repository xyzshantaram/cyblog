CURRENT:
* allow including cyblog files
* unify path handling, make relative paths relative to root from where site is being built
* better windows installer
    * better installer in general
        * options:
            1) shell script
            2) deno script with Deno.run
* github action
* allow for templating included blocks
* comments using gh issues (MAYBE)
* config file with default flags, etc
* add no-default-styles flag
* table of contents generation
* index generation

FUTURE:
* footnotes
* Extend marked renderer properly
* web frontend?
* watch dir and rebuild
